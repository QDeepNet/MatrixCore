// 1. Check the variables that in the code are only that is created
// 2. Simplify the equations \sum(\sum + \sum) rare cases
// 3. Simplify the math if they in different layers
// 4. Compute the constatnts


#include "parser.h"


void simple_optimizer(node_t *node) {

}


