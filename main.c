#include <stdint.h>
#include <stdio.h>

typedef struct {
    char *data;
    uint64_t size;
} parser;

// sum
// q_i
// 123
// * - / + _ ^ < > <= >= { } ( )
typedef struct {

}token;

int main(void) {

}